defaults:
  - workflow: phase2
  - profile: full
  - _self_

paths:
  csv: data/dataset/dataset.csv
  model: results/checkpoints/original_model_best.pt
  out: results

runtime:
  device: auto
  seed: 42

hydra:
  job:
    chdir: false